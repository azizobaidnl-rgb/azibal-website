# Production uses Vercel's automatic OIDC token for AI Gateway authentication.
# No OpenAI API key is stored in the public repository or browser code.
# VERCEL_OIDC_TOKEN is supplied automatically by Vercel at runtime.
